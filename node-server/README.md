# Node SSE API reference

정적 배포(Cloudflare assets)에서는 브라우저 **client mock**이 정본이다.

이 폴더의 `app-api/`는 이전 Node SSE 구현 참고용이다. live LLM(3주) 때
`src/app/api`로 되돌리거나 Workers 스트리밍으로 이식한다.

프로토콜은 동일: `concept-workbench-protocol.md`
